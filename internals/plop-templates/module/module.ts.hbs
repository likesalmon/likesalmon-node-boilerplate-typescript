/**
 *
 *
 * {{ kebabCase moduleName }}
 *
 *
 */
import logger from "./utils/logger";

const {{ camelCase moduleName }} = (): void => {
  logger.info("This is {{ camelCase moduleName }}.", { moduleName: "{{ kebabCase moduleName }}" });
};

export default {{ camelCase moduleName }};
