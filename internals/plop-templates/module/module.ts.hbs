/**
 *
 *
 * {{moduleName}}
 *
 *
 */
import logger from "./utils/logger";

const {{moduleName}} = (): void => {
  logger.info("This is {{moduleName}}.", { moduleName: "{{moduleName}}" });
};

export default {{moduleName}};
