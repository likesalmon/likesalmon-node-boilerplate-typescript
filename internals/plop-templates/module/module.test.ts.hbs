/**
 *
 * {{moduleName}} tests
 *
 */
import main from '../{{moduleName}}';

describe('{{moduleName}}', () => {
  describe('main', () => {
    it('should return true', async () => {
      expect(main()).toBe(true);
    });
  });
});
